# 데일리 로그 — 2026-04-30 (목)

- **Sprint**: Sprint 7 W2 이후 (핫픽스 세션, Sprint 8 없음)
- **세션**: 단일 세션 (핫픽스 + RCA + 구조 수정)
- **작성자**: Claude

---

## 오늘 한 일

### A. DeepSeek K8s ConfigMap 드리프트 수정
- **현상**: DeepSeek AI 가 드로우만 반복 → 10턴 이내 GG
- **원인**: K8s ConfigMap 이 `deepseek-v4-flash` (place rate 0%) 로 stale. Helm values.yaml 은 `deepseek-v4-pro` + `DEEPSEEK_V4_THINKING_MODE: true` 로 이미 갱신되어 있었음 (배포 드리프트)
- **수정**: `kubectl patch` + ai-adapter `rollout restart`
- **결과**: V4-Pro thinking mode 정상 동작 회복 (place rate 31.9% 기준)

### B. 프론트엔드 UI 버그 6건 (사용자 스크린샷 실측)

| # | 현상 | 수정 커밋 |
|---|------|----------|
| 1 | 서버 run 에 랙타일 추가 후 확정 불가 (freshHasInitialMeld fallback 미적용) | `9266543` |
| 2 | 타일 회수 후 드로우 비활성 (pendingGroupIds 서버 ID 잔류) | `9266543` |
| 3 | 새 그룹(2-3-4) 확정 불가 (useTurnActions.hasInitialMeld fallback 누락) | `d8872cc` |
| 4 | 조커 런(11-12-JK) 확정 불가 (computePendingScore 조커=0 계산) | `c760c04` |
| 5 | 등록완료 후 3-4-5 확정 불가 (hasInitialMeld race) | `e4b91d9` |
| 6 | 타일 전부 회수 시 서버 확정 그룹까지 사라짐 (applyMutation(null) → groups:[]) | `e4b91d9` |

### C. game-analyst RCA → P0 3건 구조적 수정 (커밋 `e4b91d9`)

3자 체제 가동: **game-analyst** (룰/RCA) + **Navigator(frontend-dev-opus)** (설계) + **Driver(frontend-dev)** (구현)

- **P0-1 (Bug #6)**: `useDragHandlers.ts` 2곳 (L509, L1095)
  - `nextTableGroups: null` → 실제 그룹 전달
  - null 전달 시 `applyMutation` 이 `groups:[]` 로 전체 보드를 클리어하던 구조적 결함 해소
- **P0-2 (Bug #1·#3·#5)**: `useWebSocket.ts` GAME_STATE 핸들러 atomic setState
  - `setPlayers()` + `setHasInitialMeld()` 분리 호출 → 단일 `useGameStore.setState()` 통합
  - hasInitialMeld 5-way SSOT race window 원천 제거
- **P0-3**: `confirmEnabled = isMyTurn`, 클릭 시 검증 토스트
  - 클라이언트 5조건 사전게이트 → 버튼 항상 활성, `handleConfirm` 에서 `setLastError` 토스트로 서버 검증 결과 노출
  - UR-15 (서버 검증 권위) 준수, false-positive 차단으로 사용자가 원인 파악 못하던 문제 해소

### 커밋 목록 (오늘)
- `9266543` fix(P0-1·2): freshHasInitialMeld fallback + pendingGroupIds 서버ID 정리
- `d8872cc` fix(P0-3): useTurnActions.hasInitialMeld fallback
- `c760c04` fix(P0-4): computePendingScore 조커 점수 계산
- `e4b91d9` fix(P0-9건): 사용자 실측 버그 5건 수정 + 데일리 마감

### 테스트 현황
- **Jest**: 640 PASS / 0 FAIL (이전 638 → +2)
- **Go**: 770 PASS (변동 없음)
- **AI Adapter**: 606 PASS (변동 없음)
- **TypeScript**: 0 errors

### 배포
- `rummikub-frontend:p0-e4b91d9` K8s Rolling Update 완료
- `rummikub-ai-adapter` ConfigMap 패치 + rollout restart

---

## 내일 할 일
- **없음** — Sprint 8 없음으로 결정. 마감 보고서 작성으로 전환

## 블로커
- 없음

## 메모

### game-analyst 근본 분석 결과
- **5-way SSOT 안티패턴**: hasInitialMeld 가 `Player.hasInitialMeld`, `gameStore.hasInitialMeld`, `selectHasInitialMeld`, `useTurnActions.hasInitialMeld`, `freshHasInitialMeld` 등 5곳에 흩어져 race window 발생. 이번 세션은 atomic setState 로 봉합했으나 근본 통합은 다음 스프린트 과제
- **null 의미 과부하**: `nextTableGroups: null` 이 "변경 없음" 인지 "전체 클리어" 인지 호출자마다 다르게 해석. typed mutation enum 으로 리팩터 필요
- **클라이언트 사전게이트 안티패턴**: confirmEnabled 5조건 사전게이트가 서버 검증과 disagree 시 사용자가 원인 파악 불가. "꼼수 쓰지 말 것" 원칙에 따라 서버 검증으로 일원화

### 세션 운영 교훈
- 표면 패치 4건 → game-analyst 가 RCA 로 동일 패턴 재발 가능성 식별 → P0 3건 구조적 수정으로 격상
- 3자 체제(Navigator/Driver/game-analyst) 가 한 세션 내 식별-설계-구현-검증 사이클 완성
- ConfigMap 드리프트는 Helm chart 만 보고 안심하면 안 됨 → ArgoCD sync drift 알림 필요 (다음 스프린트 백로그)
