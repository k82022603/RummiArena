# 세션 로그

- **날짜**: 2026-03-15
- **세션**: #03
- **시작**: 16:47
- **종료**: 18:31
- **Phase / Sprint**: Phase 2 / Sprint 1 (Day 3)

## 목표
- dev-login 엔드포인트 추가 → 브라우저 E2E 테스트 활성화
- DnD 인터랙션 고도화 (드래그 UX 개선)
- ai-adapter 테스트 커버리지 보강

## 완료한 작업

### dev-login 엔드포인트 구현 (commit: aaf15c4)

**게임 서버 (`src/game-server`)**:
- `config/config.go`: `AppEnv string` 필드 추가, `APP_ENV` 환경변수 읽기 (기본값: `"production"`)
- `handler/auth_handler.go` (NEW): `POST /api/auth/dev-login` 엔드포인트
  - APP_ENV=dev 환경에서만 동작 (프로덕션 보호)
  - userId/displayName 파라미터 수신, HS256 JWT 서명
  - 응답: `{token, userId, displayName, expiresIn: 86400}`
- `handler/auth_handler_test.go` (NEW): 6개 단위 테스트
  - 정상 로그인, missing userId, missing displayName, production 환경 차단, 빈 userId, 빈 displayName
- `cmd/server/main.go`: dev-login 라우트 등록 (APP_ENV=dev 시 활성화)

**K8s ConfigMap 패치**:
- game-server: `APP_ENV=dev` 환경변수 추가
- frontend: `GAME_SERVER_INTERNAL_URL` 설정 확인

**프론트엔드 (`src/frontend`)**:
- `lib/auth.ts`: NextAuth `CredentialsProvider("dev-login")` 추가
  - `POST /api/auth/dev-login` 호출, JWT accessToken 세션 저장
- `app/login/LoginClient.tsx`: 게스트 로그인 UI 추가
  - 닉네임 입력 (2~12자 유효성 검증), "게스트로 시작" 버튼
  - `signIn("dev-login", {userId, displayName})` 호출
- `lib/api.ts`: 모든 API 함수에 `token?: string` 파라미터 추가

**결과**: `http://localhost:30000` → 닉네임 입력 → 게임 플레이 가능 (Google OAuth 없이)

---

### DnD UX 고도화 (commit: 5c9ba7d)

**`components/tile/DraggableTile.tsx`**:
- 드래그 중 원본 위치: 반투명 + 점선 테두리 고스트 박스로 "빈 자리" 표시
- 조커/일반 타일별 색상 구분 (보라/회색)
- 박스 내부에 타일 번호/JK 실루엣 표시

**`components/tile/Tile.tsx`**:
- 조커 타일: 보라색 ring glow (`boxShadow: 0 0 0 3px #c084fc`) + ★ 배지
- 선택된 타일: ⬆ 배지 (상단 우측, mini 제외)
- Framer Motion `animate` 기존 유지

**`components/game/GameBoard.tsx`**:
- 빈 보드 플레이스홀더: 점선 박스 + "타일을 여기에 드롭하세요" + 부제
- 타일 수 배지: 각 그룹 레이블 옆에 `N개` 표시 (pending: 노란색, 확정: 회색)

**`components/game/PlayerRack.tsx`**:
- 드롭 오버 시 배경 `bg-blue-500/5` 하이라이트
- 랙 헤더: "랙 N장" → "내 타일 (N개)"로 개선

**`app/game/[roomId]/GameClient.tsx`**:
- DragOverlay: `scale: 1.12, rotate: -3` + `drop-shadow(0 10px 20px...)` 강화
- spring 애니메이션: stiffness 500, damping 20

**Frontend Docker 재빌드 + K8s 롤아웃 완료** (no-cache build, rollout restart)

---

### ai-adapter 테스트 110개 GREEN (commit: 5c9ba7d)

신규 테스트 파일 7개 추가:
- `adapter/claude.adapter.spec.ts` — Claude API 어댑터 테스트
- `adapter/deepseek.adapter.spec.ts` — DeepSeek API 어댑터 테스트
- `adapter/openai.adapter.spec.ts` — OpenAI API 어댑터 테스트
- `move/move.controller.spec.ts` — MoveController 단위 테스트
- `move/move.service.spec.ts` — MoveService 단위 테스트
- `health/health.controller.spec.ts` — HealthController 단위 테스트
- `health/health.service.spec.ts` — HealthService 단위 테스트

**결과**: 11개 test suite, 110개 테스트 ALL GREEN
- adapter 레이어: **100% statement coverage**
- 전체 statements: **31.82%** (from 0%)
- lcov.info 생성 완료 (`src/ai-adapter/coverage/lcov.info`)

## 이슈 / 블로커

### ISS-06: GitLab push 토큰 만료
| 항목 | 내용 |
|------|------|
| 심각도 | Low |
| 상태 | 미해결 (수동 조치 필요) |

glab config의 `glpat-...` 토큰이 만료됨. GitLab push 실패. GitHub push는 정상.
→ GitLab 웹 UI에서 새 PAT 발급 후 `glab auth login --hostname gitlab.com` 재실행 필요.

### ISS-07: WSL2+SWC Turbopack 로컬 빌드 패닉
| 항목 | 내용 |
|------|------|
| 심각도 | Low |
| 상태 | 우회 해결 (Docker 빌드 정상) |

`npm run build` 실행 시 SWC가 WSL2 마운트 경로를 resolve하지 못해 panic 발생.
TypeScript type check (`npx tsc --noEmit`) 는 정상. Docker 컨테이너 내 빌드는 정상.
→ 로컬 빌드는 Docker build로 대체 유지.

## 주요 결정 사항
- dev-login: APP_ENV=dev 환경 변수로 production 보호 — Sprint 2 이후에도 유지
- DraggableTile 고스트 방식: 반투명 opacity 대신 점선 박스로 교체 (더 명확한 "빈 자리" 표현)
- ai-adapter 테스트: axios mock 방식 유지 (실제 LLM 호출 없이 독립 실행 가능)

## 커밋 이력
- `aaf15c4` dev-login: 게스트 로그인 엔드포인트 + 프론트엔드 연동
- `5c9ba7d` DnD UX 개선 + ai-adapter 테스트 110개 GREEN

## 다음 세션 TODO
- **P0 (수동)**: GitLab 웹 UI에서 PAT 재발급 → `glab auth login --hostname gitlab.com` 재실행
- **P0 (수동)**: GitLab Runner 토큰 발급 → K8s Runner Helm 설치
- **P1**: 첫 GitLab CI 파이프라인 push → GREEN 확인
- **P2**: ai-adapter SonarQube lcov 커버리지 재스캔 (31.82% 반영)
- **P2**: E2E 통합 테스트 (게스트 로그인 → 방 생성 → 게임 시작)

## 메모
- dev-login으로 브라우저 E2E 완전 가능 상태 (http://localhost:30000 → 닉네임 → 플레이)
- ai-adapter adapter 레이어 100% coverage — 4개 LLM 어댑터 모두 완전 테스트
- WSL2 로컬 Next.js 빌드 Turbopack 패닉은 known issue — Docker build가 canonical
