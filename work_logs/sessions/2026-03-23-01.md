# 세션 로그

- **날짜**: 2026-03-23
- **세션**: #01
- **시작**: 09:00
- **종료**: 18:00
- **Phase / Sprint**: Phase 2 / Sprint 3 (조기 완료)

## 목표

- Sprint 3 #28~#31 전체 구현 완료
- Redis 미완성 기능 (TTL, Timer Storage, Session Storage) 구현
- 통합 테스트 실행 및 버그 수정
- Sprint 3 마감 처리

## 완료한 작업

### Redis 미완성 기능 구현 (P2/B2/C1)
- `redis_repo.go`: gameStateTTL 24h → 2h 수정 (P2)
- `ws_handler.go`: Redis Timer Storage (`game:{gameId}:timer`) 구현 (B2)
  - `saveTimerToRedis()`, `deleteTimerFromRedis()`, `restoreTimerIfNeeded()` 추가
  - 서버 재시작 시 타이머 복구: 만료됐으면 즉시 HandleTimeout, 남아있으면 잔여 시간으로 재시작
- `ws_handler.go`: Redis Session Storage (`ws:session:{userID}:{roomID}`) 구현 (C1)
  - `saveSessionToRedis()`, `deleteSessionFromRedis()` 추가
  - HandleWS 진입 시 저장, handleDisconnect 시 삭제

### ws_timer_test.go 전체 PASS
- TestStartTurnTimer_ZeroTimeout, NegativeTimeout, Expiry, CancelPrevious
- TestCancelTurnTimer_CancelsActiveTimer, NoopOnMissing
- TestTimersMu_ConcurrentAccess
- 7개 테스트 all PASS, `-race` 플래그 데이터 레이스 없음

### Sprint 3 #28 — Google OAuth K8s 환경변수
- K8s `frontend-config` ConfigMap 패치
  - `NEXT_PUBLIC_WS_URL`: K8s 내부 DNS → `ws://localhost:30080` (브라우저 접근 가능)
  - `NEXT_PUBLIC_API_URL`: K8s 내부 DNS → `http://localhost:30000/api`
- frontend deployment rollout restart 완료

### Sprint 3 #29 — GameEndedOverlay 결과 상세
- `GameClient.tsx`: GameEndedOverlay 전면 재작성
  - 승자 이름 표시, 결과 테이블(플레이어/남은 타일/승패)
  - `getPlayerDisplayName()` 헬퍼 추가 (HUMAN/AI union 타입 안전 처리)
- `gameStore.ts`: `gameOverResult: GameOverPayload | null` 필드 추가
- `useWebSocket.ts`: GAME_OVER 수신 시 `setGameOverResult(payload)` 저장

### Sprint 3 #30 — WS 재연결 백오프 수정
- `useWebSocket.ts`: `2000ms × 1.5^n` → `3000ms × 2^n`
  - 3s → 6s → 12s → 24s → 48s (더 공격적인 지수 증가로 서버 부하 방지)

### Sprint 3 #31 — gemma3:4b 프롬프트 최적화
- `ollama.adapter.ts`: `num_predict` 1024 → 256, `stop: ['\n\n', '```']`, `temperature` ≤0.7 클램프
- `prompt-builder.service.ts`: 모든 system prompt에 JSON-only 강제 문구 추가
- `buildUserPrompt()`: draw/place 형식 few-shot 예시 + `지금 즉시 JSON만 출력하라:` 추가
- `buildRetryUserPrompt()`: JSON 형식 재강조 문구 추가
- 기대 효과: 응답 시간 ~40s → ~15s, JSON 파싱 실패율 ~30% → <5%

### 통합 테스트 (사용자 여정 기반)
- 30개 TC 실행 (S1~S10 + 엣지케이스)
- 28 PASS / 1 FAIL (BUG-001) / 1 SKIP (AI Adapter LLM 직접 호출)
- `docs/04-testing/09-integration-test-report-sprint3.md` 생성

### BUG-001 수정
- `ranking_handler.go`: `GetUserRating`, `GetUserRatingHistory`에 `uuid.Parse()` 사전 검증
- 비UUID 입력 시 PostgreSQL 타입 변환 에러(500) → 400 INVALID_REQUEST
- K8s game-server 재배포 + 검증 완료 (HTTP 400 확인)

## 이슈 / 블로커

- **admin pod ErrImageNeverPull**: rummiarena/admin:dev 이미지 미빌드 상태 — Sprint 4 처리
- **WebSocket 통합 테스트 미비**: REST API만 검증됨, WS 실시간 흐름 테스트 필요
- **ConfigMap kubectl patch 미영구화**: Helm values.yaml에 반영 필요 (`NEXT_PUBLIC_WS_URL`)

## 주요 결정 사항

- `cancelTurnTimer()`: `defer h.timersMu.Unlock()` 대신 명시적 Unlock 후 Redis 호출 (deadlock 방지)
- Redis Timer/Session TTL: `wsSessionTTL = 2 * time.Hour` (gameStateTTL과 동일)
- WS 백오프 계수: 1.5 → 2 (지수 증가폭 강화)
- BUG-001 UUID 검증 위치: repository 레이어 대신 handler 레이어에서 처리 (관심사 분리)

## 커밋 이력

- `3a36d18` docs: Sprint 2 회고 + Sprint 3 킥오프 계획
- `39d3407` feat: Sprint 3 #28~#31 병렬 구현 (Redis TTL/Timer/Session + WS 개선)
- `77cdf70` security: 팀 코드 리뷰 기반 보안/품질 수정 7건
- `fe6345f` fix: ISS-001~006 버그 수정 + mock 제거 + Redis 미구현 기능 구현
- `f694e1c` feat: Sprint 3 #29~#31 — 게임 결과 오버레이 + WS 백오프 + gemma3 최적화
- `ac55e0b` fix: BUG-001 비UUID user_id 500→400 반환

## 다음 세션 TODO

- [ ] Sprint 3 회고 문서 작성 (`work_logs/retrospectives/sprint3-retrospective.md`)
- [ ] Sprint 4 킥오프 계획서 (`docs/01-planning/12-sprint4-plan.md`)
- [ ] admin 이미지 빌드 + K8s 배포 (ErrImageNeverPull 해결)
- [ ] Helm values.yaml `NEXT_PUBLIC_WS_URL: ws://localhost:30080` 영구 반영
- [ ] WebSocket 통합 테스트 시나리오 작성
- [ ] Sprint 4 GitHub Issues #32~#35 등록

## 메모

- Sprint 3 원래 시작일: 2026-03-29 → 실제 완료일: 2026-03-23 (6일 조기 완료)
- Sprint 3 완료 속도: 30 SP를 하루 만에 완료 (Sprint 2 50 SP × 3일에 이은 기록)
- 병렬 에이전트(frontend-dev + ai-engineer) 동시 실행으로 #29/#30/#31 병렬 처리 성공
