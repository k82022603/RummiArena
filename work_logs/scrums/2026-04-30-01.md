# 스크럼 미팅 로그

- **날짜**: 2026-04-30
- **Sprint**: Sprint 7 W2 이후 (핫픽스 세션, Sprint 8 없음)
- **참석자**: 애벌레, Claude

## 각자 공유 (3가지)

### 애벌레
- **어제**: Sprint 7 W2 마감 처리 (실측 9건 RCA → 수정 → 배포 → 일일 마감)
- **오늘**: DeepSeek 드로우만 하는 회귀 보고 + 프론트 UI 버그 6건 스크린샷 보고. game-analyst 근본 분석 지시
- **블로커**: 없음 (보고 → 수정 → 배포까지 한 세션 내 완료)

### Claude
- **어제**: P3-3 Sub-C/D, selectHasPending SSOT 핫픽스, 9건 처리
- **오늘**: A) DeepSeek K8s ConfigMap 드리프트 수정, B) UI 버그 6건 표면 수정, C) game-analyst RCA 결과 P0 3건 구조적 재수정
- **블로커**: 없음 (메인 세션 직접 수정 + Navigator/Driver/game-analyst 3자 체제 안정화)

## 논의 사항

### A. DeepSeek K8s ConfigMap 드리프트 (10턴 GG 회귀)

| 항목 | 값 |
|------|-----|
| 현상 | DeepSeek 턴마다 드로우만 → 10턴 이내 GG |
| 원인 | K8s ConfigMap이 `deepseek-v4-flash`(place rate 0%) 잔존. Helm values.yaml은 이미 `deepseek-v4-pro` + `DEEPSEEK_V4_THINKING_MODE: true` |
| 분류 | 배포 드리프트 (Helm chart vs 실제 ConfigMap 불일치) |
| 수정 | `kubectl patch` ConfigMap + ai-adapter rollout restart |
| 검증 | V4-Pro thinking 정상 동작, place rate 31.9% (N=3 기준) 회복 |

> 후속 조치: ArgoCD sync drift 감지 알림 활성화 검토 (다음 스프린트 백로그)

### B. 프론트엔드 UI 버그 6건 (사용자 실측)

| # | 현상 | 원인 | 커밋 |
|---|------|------|------|
| 1 | 서버 run에 내 랙타일 추가 후 확정 불가 | `freshHasInitialMeld` fallback 미적용 → 신규 그룹 분기 오진입 | `9266543` |
| 2 | 타일 회수 후 드로우 버튼 비활성 | `pendingGroupIds`에 서버 그룹 ID 잔류 | `9266543` |
| 3 | 새 그룹(2-3-4) 확정 불가 | `useTurnActions.hasInitialMeld` fallback 누락 | `d8872cc` |
| 4 | 조커 포함 런(11-12-JK) 확정 불가 | `computePendingScore`에서 조커=0 계산 (UI는 30점 표시) | `c760c04` |
| 5 | 등록완료 후 3-4-5 확정 불가 | hasInitialMeld race + 클라이언트 게이트 복합 | `e4b91d9` |
| 6 | 타일 전부 회수 시 서버 확정 그룹까지 사라짐 | `applyMutation(null)` → `groups:[]` 전체 클리어 | `e4b91d9` |

### C. game-analyst RCA → P0 3건 구조적 수정 (커밋 `e4b91d9`)

표면 패치 4건(#1·#2·#3·#4) 후, **game-analyst** 가 동일 패턴이 재발할 수 있는 근본 원인 3건을 식별. Navigator(frontend-dev-opus) 설계 + Driver(frontend-dev) 구현 + game-analyst 룰 검증 3자 체제로 처리.

| P0 | 대상 버그 | 근본 원인 | 구조적 수정 |
|----|----------|----------|------------|
| **P0-1** | #6 (보드 클리어) | `nextTableGroups: null` 의미 과부하. null 전달 시 `applyMutation` 이 `groups:[]` 로 전체 클리어 | useDragHandlers.ts 2곳(L509, L1095) null → 실제 그룹 전달 |
| **P0-2** | #1·#3·#5 | hasInitialMeld 5-way SSOT race window. `setPlayers()` + `setHasInitialMeld()` 분리 호출 사이에 다른 컴포넌트 리렌더 발생 시 inconsistent state | useWebSocket.ts GAME_STATE 핸들러 단일 `useGameStore.setState()` 통합 (atomic) |
| **P0-3** | 클라이언트 false-positive 차단 | confirmEnabled 5조건 사전게이트가 서버 검증과 disagree 시 버튼 자체가 사라져 사용자가 원인 파악 불가 | `confirmEnabled = isMyTurn`, 클릭 시 `setLastError` 토스트로 검증 결과 노출. UR-15(서버 검증 권위) 준수 |

### 게임룰 SSOT 검증 (game-analyst)
- 6건 모두 기존 룰(V-06/V-09/UR-15/UR-24) 매핑 완료
- 신규 룰 제안: 없음
- "5-way SSOT" 자체가 아키텍처 부채 → 다음 스프린트 검토 (현재는 atomic setState 로 race window 봉합)

### 테스트
- **Jest**: 640 PASS / 0 FAIL (이전 638 → +2 assertion 보강)
- **Go**: 770 PASS (변동 없음)
- **AI Adapter**: 606 PASS (변동 없음)
- **TypeScript**: 0 errors
- **K8s**: `rummikub-frontend:p0-e4b91d9` Rolling Update 완료

## 액션 아이템

| 담당 | 할 일 | 기한 |
|------|-------|------|
| 애벌레 | localhost:30000 실측 검증 (6건 + DeepSeek) | 즉시 |
| Claude | 일일 마감 처리 (daily-close) | 오늘 |
| 보류 | ArgoCD sync drift 감지 알림 활성화 | 다음 스프린트 |
| 보류 | hasInitialMeld 5-way SSOT 통합 (Player.hasInitialMeld 단일화) | 다음 스프린트 |
| 보류 | useDragHandlers null 의미 과부하 리팩터 (typed mutation enum) | 다음 스프린트 |

## 메모
- Sprint 8 없음 결정 유지 — 핫픽스 세션으로만 처리
- game-analyst 의 RCA 가 표면 패치 → 구조적 수정으로 격상시킨 결정적 역할. 3자 체제(Navigator/Driver/game-analyst) 가 P0 식별-설계-구현-검증 사이클을 한 세션에 압축
- 사용자 피드백: "꼼수 쓰지 말 것" 원칙 준수 — P0-3 에서 클라이언트 사전게이트 제거하고 서버 검증으로 일원화
