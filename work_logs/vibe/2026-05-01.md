# 바이브 로그 — 2026-05-01 (금)

오늘 두 가지 이야기가 하나로 겹쳤다.

오전에는 LLaMA 소원이 이루어졌다. qwen2.5:3b는 처음부터 place rate 0%였다. 타일 13장에서 멜드 조합을 찾는 건 3B 파라미터 모델의 능력 밖이었다. 그래서 방향을 바꿨다. 모델에게 탐색을 맡기지 말고, 게임 엔진이 사전 계산한 후보 목록을 프롬프트에 넣어주는 것. 모델의 역할을 "조합 탐색"에서 "후보 선택"으로 좁혔다. 결과는 15.8%. 완벽하진 않지만, 0에서 15.8은 무한대의 개선이다.

오후에는 사용자 스크린샷이 들어왔다. Room 927a6635. 배진용과 JinYong Bae, 두 Human이 게임을 하다가 확정 버튼이 계속 잠겨있었다. 클릭이 안 된다. 첫 번째 턴에서 성공한 뒤로 영원히 열리지 않는 자물쇠가 되어버렸다. 덕분에 LLaMA가 이겼다. AI가 Human을 이긴 게 아니라, 버그가 Human을 이긴 것이다.

원인을 파악하는 데 시간이 걸렸다. `confirmBusy`를 해제하는 조건이 `!draftPendingTableGroups` — draft 객체가 null일 때만 해제된다. 그런데 한 번 draft가 생성되면, 이후 turn이 넘어가도 draft는 계속 살아있다. reset()을 해도 내용물만 비워질 뿐 객체 자체는 남는다. 그러니 confirmBusy는 영원히 true. 상태 전이를 boolean 값 하나로 제어하려 했을 때 생기는 고전적인 함정이다.

그리고 오늘의 세 번째 이야기: 같은 맥락으로, GameClient에 109줄짜리 handleConfirm이 있었는데 이게 실제로는 쓰이지 않는 useTurnActions.handleConfirm과 병렬로 존재했다. 두 곳에 검증 로직이 흩어져 있으면 한쪽이 오래되어도 알아채기 어렵다. 조커 갭 버그가 그 증거였다. 하나로 통합하고 나니 109줄이 5줄이 되었다. 줄이 줄어서 좋은 게 아니라, 진실이 한 곳에 모인 것이 좋은 거다.

오늘 `/daily-close` 커맨드를 쓰지 않았다는 걸 사용자가 짚어줬다. 습관이 아직 안 된 것이다. 커맨드 체계를 먼저 확인하는 것도 루틴의 일부.
