# Dependencies
node_modules/
vendor/

# Build outputs
dist/
build/
.next/
out/
# Docker image exports (로컬 빌드 tar 산출물)
admin.tar
*-image.tar
# Helm chart packages (helm package 명령의 산출물, 릴리스 시 수동 업로드)
helm/charts/*.tgz

# Go binaries
src/game-server/server

# Environment
.env
.env.local
.env.*.local
.env.cicd

# MCP config (contains tokens)
.mcp.json

# MCP config (contains tokens)
.mcp.json

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Docker
docker-compose.override.yml

# Kubernetes secrets (never commit)
*-secret.yaml
*-secrets.yaml

# SonarQube
.scannerwork/

# Test coverage
coverage/
*.lcov
# Playwright root-level test-results (src/frontend/test-results/ 는 CLAUDE.md 정책상 추적 대상)
/test-results/

# Claude Code 로컬 도구 (워크트리는 세션별 임시 디렉토리)
.claude/worktrees/

# Logs (application runtime logs only)
*.log
# Note: /logs/ 디렉토리는 프로젝트 기록용이므로 추적함

# Secrets & Credentials
*.pem
*.key
*.p12
*.pfx
*.jks
credentials*.json
client_secret*
secrets/
kubeconfig
*.kubeconfig
.env.production
.env.staging
.docker/
# Secret mapping files (dev/staging secret → placeholder 매핑 테이블)
secret-replacements*
*secret-replacements*
scripts/secret-replacements.txt

# Temporary
tmp/
temp/
