# .gitlab-ci.yml
# RummiArena GitLab CI/CD 파이프라인
# DevSecOps: lint → test → quality → build → update-gitops
#
# 교대 실행 전략 (10GB WSL 제약):
#   CI 모드: PG(Sonar) + SonarQube + GitLab Runner ~ 2.5 GB
#   Dev/Deploy 모드와 CI 모드 동시 실행 금지
#
# 필요한 GitLab CI/CD Variables (Settings → CI/CD → Variables):
#   SONAR_HOST_URL       — SonarQube 서버 URL (예: http://host.docker.internal:9001)
#   SONAR_TOKEN          — SonarQube 분석 토큰
#   CI_REGISTRY_USER     — GitLab Container Registry 사용자 (자동 제공)
#   CI_REGISTRY_PASSWORD — GitLab Container Registry 패스워드 (자동 제공)
#   GITOPS_TOKEN         — GitHub Personal Access Token (GitOps repo push용)

# =============================================================================
# 전역 설정
# =============================================================================

stages:
  - lint
  - test
  - quality
  - build
  - update-gitops

variables:
  DOCKER_IMAGE: registry.gitlab.com/$CI_PROJECT_PATH
  DOCKER_DRIVER: overlay2
  DOCKER_TLS_CERTDIR: ""          # DinD TLS 비활성화 (로컬 러너)
  SONAR_PROJECT_KEY: rummiarena
  # Go 모듈 캐시 경로
  GOPATH: "$CI_PROJECT_DIR/.cache/go"
  # Node.js npm 캐시
  npm_config_cache: "$CI_PROJECT_DIR/.cache/npm"
  # Trivy DB 캐시 (매 실행 DB 재다운로드 방지)
  TRIVY_CACHE_DIR: "$CI_PROJECT_DIR/.cache/trivy"

# 공통 캐시 앵커 (YAML 앵커 재사용)
.go-cache: &go-cache
  key:
    files:
      - src/game-server/go.sum
  paths:
    - .cache/go/

.node-ai-cache: &node-ai-cache
  key:
    files:
      - src/ai-adapter/package-lock.json
  paths:
    - src/ai-adapter/node_modules/
    - .cache/npm/

.node-fe-cache: &node-fe-cache
  key:
    files:
      - src/frontend/package-lock.json
  paths:
    - src/frontend/node_modules/
    - .cache/npm/

# =============================================================================
# Lint Stage — 코드 스타일 및 정적 문법 검사
# =============================================================================

lint-go:
  stage: lint
  image: golangci/golangci-lint:v2.1
  cache:
    <<: *go-cache
    policy: pull-push
  script:
    - cd src/game-server
    - golangci-lint run ./... --timeout 5m
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"
  interruptible: true

lint-nest:
  stage: lint
  image: node:20-alpine
  cache:
    <<: *node-ai-cache
    policy: pull-push
  script:
    - cd src/ai-adapter
    - npm ci --prefer-offline
    - npm run lint
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"
  interruptible: true

lint-frontend:
  stage: lint
  image: node:20-alpine
  cache:
    <<: *node-fe-cache
    policy: pull-push
  script:
    - cd src/frontend
    - npm ci --prefer-offline
    - npm run lint
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"
  interruptible: true

# =============================================================================
# Test Stage — 단위/통합 테스트 + 커버리지 수집
# =============================================================================

test-go:
  stage: test
  image: golang:1.22-alpine
  cache:
    <<: *go-cache
    policy: pull
  before_script:
    - apk add --no-cache gcc musl-dev
  script:
    - cd src/game-server
    # 커버리지 프로파일 수집 (cobertura XML은 go-junit-report로 변환)
    - go test ./... -v
        -coverprofile=coverage.out
        -covermode=atomic
        -timeout 120s
    - go tool cover -func=coverage.out
    # XML 변환 (GitLab Coverage 리포트)
    - go install github.com/boumenot/gocover-cobertura@latest
    - gocover-cobertura < coverage.out > coverage.xml
  coverage: '/total:\s+\(statements\)\s+(\d+\.\d+)%/'
  artifacts:
    reports:
      coverage_report:
        coverage_format: cobertura
        path: src/game-server/coverage.xml
    paths:
      - src/game-server/coverage.out
      - src/game-server/coverage.xml
    expire_in: 7 days
  rules:
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"

test-nest:
  stage: test
  image: node:20-alpine
  cache:
    <<: *node-ai-cache
    policy: pull
  script:
    - cd src/ai-adapter
    - npm ci --prefer-offline
    - npm run test:cov
  artifacts:
    reports:
      coverage_report:
        coverage_format: cobertura
        path: src/ai-adapter/coverage/cobertura-coverage.xml
    paths:
      - src/ai-adapter/coverage/
    expire_in: 7 days
  rules:
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"

# =============================================================================
# Quality Stage — 보안 게이트 (DevSecOps)
# sonarqube + trivy-fs 는 병렬 실행
# =============================================================================

sonarqube:
  stage: quality
  image: sonarsource/sonar-scanner-cli:latest
  variables:
    SONAR_HOST_URL: $SONAR_HOST_URL   # GitLab CI Variable로 주입
    SONAR_TOKEN: $SONAR_TOKEN         # GitLab CI Variable로 주입
    # SonarQube Scanner JVM 메모리 최소화 (CI 모드 10GB 제약)
    SONAR_SCANNER_OPTS: "-server -Xmx512m"
  script:
    - sonar-scanner
        -Dsonar.projectKey=$SONAR_PROJECT_KEY
        -Dsonar.projectName="RummiArena"
        -Dsonar.host.url=$SONAR_HOST_URL
        -Dsonar.login=$SONAR_TOKEN
        -Dsonar.qualitygate.wait=true
        -Dsonar.sources=src/
        -Dsonar.exclusions="**/node_modules/**,**/*.test.go,**/*.spec.ts,**/*.spec.js,**/dist/**,**/coverage/**"
        -Dsonar.go.coverage.reportPaths=src/game-server/coverage.out
        -Dsonar.javascript.lcov.reportPaths=src/ai-adapter/coverage/lcov.info,src/frontend/coverage/lcov.info
  needs:
    - test-go
    - test-nest
  allow_failure: false
  rules:
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH == "develop"

trivy-fs:
  stage: quality
  image:
    name: aquasec/trivy:latest
    entrypoint: [""]
  cache:
    key: trivy-db-cache
    paths:
      - .cache/trivy/
    policy: pull-push
  variables:
    TRIVY_CACHE_DIR: ".cache/trivy"
  script:
    # 파일시스템 취약점 스캔 (HIGH/CRITICAL 발견 시 파이프라인 실패)
    - trivy fs
        --exit-code 1
        --severity HIGH,CRITICAL
        --no-progress
        --format table
        --cache-dir "$TRIVY_CACHE_DIR"
        --skip-dirs "src/ai-adapter/node_modules,src/frontend/node_modules,.cache"
        src/
  allow_failure: false
  rules:
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
  interruptible: true

# =============================================================================
# Build Stage — Docker 멀티스테이지 빌드 + 이미지 스캔
# =============================================================================

.build-common: &build-common
  stage: build
  image: docker:26-dind
  services:
    - name: docker:26-dind
      alias: docker
  before_script:
    - docker login -u "$CI_REGISTRY_USER" -p "$CI_REGISTRY_PASSWORD" "$CI_REGISTRY"
  rules:
    - if: $CI_COMMIT_BRANCH == "main"

build-game-server:
  <<: *build-common
  script:
    - docker build
        --target production
        --tag $DOCKER_IMAGE/game-server:$CI_COMMIT_SHA
        --tag $DOCKER_IMAGE/game-server:latest
        --label "org.opencontainers.image.revision=$CI_COMMIT_SHA"
        --label "org.opencontainers.image.ref.name=$CI_COMMIT_REF_NAME"
        --cache-from $DOCKER_IMAGE/game-server:latest
        src/game-server/
    - docker push $DOCKER_IMAGE/game-server:$CI_COMMIT_SHA
    - docker push $DOCKER_IMAGE/game-server:latest
    # Trivy 이미지 스캔 (CRITICAL 발견 시 실패)
    - docker run --rm
        -v /var/run/docker.sock:/var/run/docker.sock
        aquasec/trivy:latest image
        --exit-code 1
        --severity CRITICAL
        --no-progress
        $DOCKER_IMAGE/game-server:$CI_COMMIT_SHA

build-ai-adapter:
  <<: *build-common
  script:
    - docker build
        --target production
        --tag $DOCKER_IMAGE/ai-adapter:$CI_COMMIT_SHA
        --tag $DOCKER_IMAGE/ai-adapter:latest
        --cache-from $DOCKER_IMAGE/ai-adapter:latest
        src/ai-adapter/
    - docker push $DOCKER_IMAGE/ai-adapter:$CI_COMMIT_SHA
    - docker push $DOCKER_IMAGE/ai-adapter:latest
    - docker run --rm
        -v /var/run/docker.sock:/var/run/docker.sock
        aquasec/trivy:latest image
        --exit-code 1
        --severity CRITICAL
        --no-progress
        $DOCKER_IMAGE/ai-adapter:$CI_COMMIT_SHA

build-frontend:
  <<: *build-common
  script:
    - docker build
        --target production
        --tag $DOCKER_IMAGE/frontend:$CI_COMMIT_SHA
        --tag $DOCKER_IMAGE/frontend:latest
        --cache-from $DOCKER_IMAGE/frontend:latest
        src/frontend/
    - docker push $DOCKER_IMAGE/frontend:$CI_COMMIT_SHA
    - docker push $DOCKER_IMAGE/frontend:latest
    - docker run --rm
        -v /var/run/docker.sock:/var/run/docker.sock
        aquasec/trivy:latest image
        --exit-code 1
        --severity CRITICAL
        --no-progress
        $DOCKER_IMAGE/frontend:$CI_COMMIT_SHA

# =============================================================================
# Update GitOps Stage — ArgoCD가 감시하는 dev-values.yaml 이미지 태그 업데이트
# =============================================================================

update-gitops:
  stage: update-gitops
  image: alpine/git:latest
  before_script:
    - git config --global user.email "ci@rummiarena.local"
    - git config --global user.name "GitLab CI Bot"
  script:
    - git clone https://oauth2:$GITOPS_TOKEN@github.com/k82022603/RummiArena.git gitops
    - cd gitops
    - 'sed -i "s|registry.gitlab.com/k82022603/rummiarena/game-server:.*|registry.gitlab.com/k82022603/rummiarena/game-server:$CI_COMMIT_SHA|g" helm/environments/dev-values.yaml'
    - 'sed -i "s|registry.gitlab.com/k82022603/rummiarena/ai-adapter:.*|registry.gitlab.com/k82022603/rummiarena/ai-adapter:$CI_COMMIT_SHA|g" helm/environments/dev-values.yaml'
    - 'sed -i "s|registry.gitlab.com/k82022603/rummiarena/frontend:.*|registry.gitlab.com/k82022603/rummiarena/frontend:$CI_COMMIT_SHA|g" helm/environments/dev-values.yaml'
    - git add helm/environments/dev-values.yaml
    - 'git diff --staged --quiet || git commit -m "ci: update image tags to $CI_COMMIT_SHA [skip ci]"'
    - git push
  rules:
    - if: $CI_COMMIT_BRANCH == "main"
  when: on_success
  needs:
    - build-game-server
    - build-ai-adapter
    - build-frontend
